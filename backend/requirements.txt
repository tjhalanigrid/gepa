# Vehicle Damage Assessment — backend (FastAPI) dependencies.
#
# Run the server from the repository root:
#     uvicorn backend.app:app --host 0.0.0.0 --port 8000
#
# The API calls into pipeline/ and models/, so the model-runtime packages
# (torch via ultralytics, ollama) are required at runtime too. Heavy ML deps are
# also pinned in the repo-root requirements-base.txt — install both for a full env.

# Web framework
fastapi
uvicorn[standard]
python-multipart

# Config & validation
pyyaml
pydantic>=2

# Persistence (PostgreSQL)
sqlalchemy>=2.0
psycopg2-binary
python-dotenv

# Imaging
pillow
numpy
opencv-python

# Model runtime (used via pipeline/ and models/)
ollama
ultralytics
